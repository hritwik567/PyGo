Follow the following steps for running the IR Generator:

-> First go to the src folder.

-> Run the following command to run the parser.

    $ ./parser.py ../tests/sometest.go --out=somefile.ir

Dependencies:
    ply
    pprint
    csv
    copy
